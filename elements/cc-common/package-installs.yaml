---
build-essential:
  when: DISTRO_NAME = ubuntu
git:
jq:
nfs-common:
linux-generic:
  when: DISTRO_NAME = ubuntu
# ignore dependency
# https://bugs.launchpad.net/ubuntu/+source/flash-kernel/+bug/1914712
flash-kernel-:
  arch: arm64