---
python39:
  when: DISTRO_NAME = centos
python3-dev:
python3-pip:
  phase: pre-install.d
python3-setuptools:
python3-venv:
  when: DISTRO_NAME = ubuntu
python3-virtualenv:
  when: DISTRO_NAME = centos
