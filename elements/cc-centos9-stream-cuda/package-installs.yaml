---
# These iptables packages are required for the firewall to interface properly with the kernel module
iptables-nft:
iptables-utils:
dhcp-client:
redhat-rpm-config:
rpm-build:
rpm-sign:
nscd:
chrony:
cuda-12-6: